
    Anhang zur Sitzung vom 07.03.2019:

    Packete, welche zu implementieren sind bis am 12.03.2019:


        LOGIN { login } (Viktor)
        PING { ping und pong } (Sanja)
        CHAT { Send message, receive message } (Moritz)
        NAME { Set Name, Get name, Push name } (Viktor)
        LOGOUT { Send Logout, Push Logout } (Viktor)
        LOBBIES { See lobbies, show lobbies, join lobby, leave lobby, create lobby } (Sebastian)

    Noch zu implementieren:

        Server Player Klasse { Name, Thread-Nr., Ping } (Viktor)
